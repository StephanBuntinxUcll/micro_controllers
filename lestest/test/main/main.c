#include <stdio.h>
#include "myAX.h"



int teller = 0;

void app_main(void)
{
    myAX_setup();
    int waarde;
    while(1)
    {
        while = MyAinlzez
        waarde = myAX_inlezen();
        printf("Waarde: %d\n", waarde);

        vtaskDelay(100);
    }

}
